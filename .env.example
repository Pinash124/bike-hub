# Backend base URL used by frontend at runtime (production and optional local direct call)
VITE_API_BASE_URL=https://bikehub-production-731a.up.railway.app

# Optional: explicit Vite dev proxy target (if omitted, uses VITE_API_BASE_URL)
VITE_API_PROXY_TARGET=https://bikehub-production-731a.up.railway.app
